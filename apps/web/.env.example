# ─────────────────────────────────────────────────────────────────────────────
# Este archivo es CORTO a propósito.
#
# El correo, el cobro y la dirección pública se configuran DENTRO del sistema,
# en /panel/configuracion, y se guardan en la base de datos. Aquí solo va lo
# que hace falta ANTES de poder leer esa base de datos.
#
# Lo que siga escrito abajo, comentado, se sigue leyendo como RESPALDO para las
# instalaciones antiguas. Si el mismo valor está guardado en el panel, manda el
# panel.
# ─────────────────────────────────────────────────────────────────────────────

# Origen de datos: 'json' (data/invitations.json) o 'database' (PostgreSQL)
DATA_SOURCE="json"

# Base de datos
DATABASE_URL="postgresql://usuario:contraseña@host:5432/citas?schema=public"

# Acceso. El seed crea este superadministrador.
SUPERADMIN_EMAIL="admin@citas.local"

# Llave de cifrado de los secretos guardados (la contraseña del SMTP, la clave
# de Whish). Esta NO puede vivir en la base de datos: sería guardar la llave
# dentro del cajón que abre. Se crea con:
#   openssl rand -base64 32 > /etc/citas/secret.key && chmod 600 /etc/citas/secret.key
CITAS_SECRET_KEY_FILE="/etc/citas/secret.key"

# Render del PNG: ruta al binario de Chrome/Chromium si no está en una ruta habitual
# CHROMIUM_PATH="/usr/bin/chromium"


# ── Respaldo: todo lo de aquí abajo se configura mejor en /panel/configuracion ──
#
# Se sigue leyendo para no romper una instalación que ya lo tenga puesto, y para
# poder arrancar un entorno de pruebas sin base de datos. Nada de esto es
# necesario si el sistema ya está montado.

# Dominio público, para las vistas previas al compartir
# NEXT_PUBLIC_SITE_URL="https://…"

# Cobro. 'mock' solo en desarrollo; el código lo prohíbe en producción.
# PAYMENTS_PROVIDER="whish"
# WHISH_BASE_URL="https://api.whish.money/itel-service/api"
# WHISH_CHANNEL=""
# WHISH_WEBSITE_URL="citas.posxml.com"
# WHISH_SECRET_ENC="v1....."

# Correo saliente. 'smtp' envía de verdad; cualquier otro valor imprime en el
# log y se niega a arrancar en producción.
# MAILER="smtp"
# SMTP_HOST="smtp.tu-proveedor.com"
# SMTP_PORT="587"
# SMTP_USER="info@tu-dominio.com"
#
# OJO: el remitente se llama MAIL_FROM, NO SMTP_FROM. Escribir SMTP_FROM es un
# error que ya costó un despliegue: el envío revienta al primer código.
# MAIL_FROM="Tu Marca <info@tu-dominio.com>"
#
# OJO: aquí va el valor CIFRADO, que empieza por "v1.". Pegar la contraseña en
# claro en esta línea es el otro error que ya costó un despliegue: parece
# configurado y solo falla cuando alguien intenta entrar. Se cifra con:
#   printf %s 'la-contraseña' | npm run secret:encrypt --workspace @citas/web
# SMTP_PASSWORD_ENC="v1....."
#
# Las dos equivocaciones las caza /panel/sistema antes de que nadie intente
# entrar, y nombra la variable exacta que está mal.

# ─────────────────────────────────────────────────────────────────────────────
# Almacén de las fotos del directorio (compatible con S3).
#
# NO SE ESCRIBEN A MANO: las pone `sudo bash deploy/minio-instalar.sh`, que
# levanta MinIO en esta misma máquina, genera las credenciales con
# /dev/urandom, cifra la secreta y añade estas seis líneas al .env.
#
# Aquí van los NOMBRES y ningún valor, como en todo este archivo. Una
# credencial de ejemplo es una credencial que alguien deja puesta.
#
# Sin las seis, el almacén es el de MEMORIA — que en producción se NIEGA a
# arrancar, así que una instalación sin bucket falla ruidosamente en vez de
# decir «foto guardada» y tirarla. /panel/sistema lo marca en rojo.
#
# El endpoint apunta al BUCLE LOCAL a propósito: quien llegue a ese puerto con
# estas credenciales lee y escribe las fotos de todos los proveedores. Es la
# misma razón por la que la web escucha en 127.0.0.1 y por la que la dirección
# del servicio de WhatsApp no puede salir de ahí.
#
# Sirve igual cualquier otro compatible con S3 —R2, Amazon, Backblaze B2—:
# cambian estas seis y nada más. Con R2, STORAGE_REGION es "auto".
#
# STORAGE_ENDPOINT="http://127.0.0.1:9000"
# STORAGE_REGION="us-east-1"
# STORAGE_BUCKET="citas-directorio"
# STORAGE_ACCESS_KEY_ID=""
#
# CIFRADA, como la del SMTP: empieza por "v1.". En claro solo fuera de
# producción, y el código lo impide allí.
# STORAGE_SECRET_ACCESS_KEY_ENC="v1....."
#
# Opcional y hoy sin usar: la base pública para el día que haya CDN. Las fotos
# se sirven por /api/d/media/<id>, que comprueba el estado antes de devolver un
# byte — una dirección directa se saltaría esa comprobación.
# STORAGE_PUBLIC_BASE_URL=""
